user-track-api {
  server {
    host = "0.0.0.0"
    port = 8080
  }

  db {
    driver = "org.postgresql.Driver"
    jdbc-url = "jdbc:postgresql://localhost:5432/postgres"
    user = "postgres",
    password = "postgres"
    threads = 32
  }
}